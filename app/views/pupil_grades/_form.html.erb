<%= form_with(model: pupil_grade, local: true) do |form| %>
  <% if pupil_grade.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(pupil_grade.errors.count, "error") %> prohibited this pupil_grade from being saved:</h2>

      <ul>
      <% pupil_grade.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
